Скажи, а сложно добавить в этот код функцию, которую бы можно было передать в движок калькулятора как callback, чтобы она открывала файловый диалог и возвращала полное имя выбранного файла, что-то вроде bool FileDlg(char *fName)? Это не приведет к новым зависимостям и раздуванию кода?